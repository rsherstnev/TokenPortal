<?php
defined('BASEPATH') OR exit('No direct script access allowed');

/*
|--------------------------------------------------------------------------
| Журнал действий пользователей (аудит)
|--------------------------------------------------------------------------
|
| audit_log_enabled — записывать ли события (FALSE отключает запись).
| audit_log_file      — абсолютный или относительный путь к файлу на сервере.
|                       Каждая строка файла — один JSON-объект (JSON Lines).
|                       Каталог должен быть доступен для записи веб-серверу.
|
*/
$config['audit_log_enabled'] = TRUE;
// Путь вне домашнего каталога: mod_php (www-data) часто не может писать в /home/…
// (политика ОС / sandbox), хотя чтение приложения работает. /var/log/ — типичный вариант.
$config['audit_log_file'] = '/var/log/skzi-tokens/audit_actions.jsonl';
